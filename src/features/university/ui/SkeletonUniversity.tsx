import Skeleton from '@/shared/ui/Skeleton'

export default function SkeletonUniversity() {
  return (
    <Skeleton
      key={`skeleton-university}`}
      className="h-4.5 w-[120px] rounded-[20px]"
    />
  )
}
